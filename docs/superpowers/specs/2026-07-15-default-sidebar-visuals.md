# 默认主题侧栏视觉增强设计

## 目标

仅为“当前默认主题”补齐参考图中的侧栏图标、油田背景及金色选中态，不改变路由、菜单顺序、布局尺寸或既有功能。

## 方案

- 现有菜单配置为每个页面关联一个 Lucide 图标，并在导航按钮内显示；图标不参与导航逻辑。
- 生成一张无文字的深色油田抽油机背景图，固定显示于默认主题侧栏底部，并以暗色渐变压暗。
- 默认主题的激活项使用金色描边、轻微暗金底色与柔和光晕；其他四套主题维持原有样式。

## 验证

- 补充源代码级回归测试，确认默认主题限定样式、图标映射与背景资源存在。
- 运行测试、Lint 与构建。
